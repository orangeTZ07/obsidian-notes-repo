![[Pasted image 20260413182119.png]]
这道题实际上在考察sql和php的 #宽松比较 特性
思路1：
- 本题没有过滤 `|` ，所以可以用 `||` 代替 `or`
- 本题后端逻辑使用php，怀疑存在宽松比较，输入密码 `0` 发现成功绕过后端逻辑，并且成功绕过sql比较，返回了字符开头的字符串(flag)
思路2：
- 直接用户名输入0, 匹配所有字符开头用户名
- 密码输入0
#sql宽松比较 #php宽松比较 