# Lab 01：基础回显型 SSRF

## 目标
学会识别 SSRF、验证可控、读取本地敏感路径。

## 靶场设定（抽象）
- 参数：`?url=`
- 后端会抓取 URL 并返回内容。

## 过关步骤
1. 用 `http://你的webhook` 验证服务端请求。
2. 请求 `http://127.0.0.1/` 观察回显差异。
3. 枚举 `/admin`、`/flag`、`/debug`。

## 复盘重点
- 你是否先验证“服务端请求”再打内网？
- 你是否记录了状态码和长度差异？
