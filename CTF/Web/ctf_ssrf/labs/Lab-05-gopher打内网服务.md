# Lab 05：gopher 打内网服务

## 目标
用 gopher 构造原始协议与内网服务交互。

## 靶场设定（抽象）
- `http/https` 可用，`gopher` 也放开。
- 内网存在可利用服务（如 Redis）。

## 过关步骤
1. 用最短 gopher payload 验证端口联通。
2. 构造单命令再到多命令链。
3. 回收证据（文件、键值、服务响应）。

## 复盘重点
- 失败是出在编码、协议格式还是权限？
- 你的 payload 是否分阶段验证？
